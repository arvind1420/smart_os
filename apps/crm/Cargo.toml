[package]
name = "crm"
version = "0.1.0"
edition = "2024"

[dependencies]
smartsdk = { path = "../../smartsdk" }
smartpack = { path = "../../smartpack", default-features = false, features = ["alloc"] }

[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"
opt-level = "z"
lto = true
